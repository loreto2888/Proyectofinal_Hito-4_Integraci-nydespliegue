# Pendientes de implementacion

## Prioridad alta

## Completado recientemente
- Agregadas pruebas del backend con mocks para auth, usuarios, publicaciones, favoritos y carrito, cubriendo validaciones y flujos principales de endpoints faltantes.
- Mejoradas las validaciones y los mensajes de error en login, registro y creacion/edicion de publicaciones en frontend y backend.
- Renombrado visualmente el campo `Título` a `Artículo` en la interfaz de crear y editar publicaciones.
- Confirmado que el formulario de registro ya incluye un campo explicito de nombre.
- Redirigido `Cerrar sesion` al login con mensaje de confirmacion.
- Redirigidos `login` y `registro` al menu principal con mensaje de confirmacion.
- Implementado carrito de compras en frontend y backend.
- Habilitado agregar productos al carrito desde el listado y desde el detalle de publicaciones.
- Habilitada la eliminacion de productos del carrito.
- Implementado flujo de simulacion de compra o pago con vaciado de carrito y descuento de stock.
- Corregido el mapeo de datos entre backend y frontend para publicaciones.
- Ajustado `PostCard` para usar los campos reales devueltos por la API.
- Ajustado `PostDetail` para usar los campos reales devueltos por la API y cargar siempre el detalle completo.
- Corregido `Profile` para listar correctamente las publicaciones del usuario autenticado.
- Ajustado `NewPost` para que el boton de volver vaya explicitamente a la pagina principal.
- Agregado y unificado el boton `Volver al menu principal` en las secciones requeridas del frontend.
- Implementada edicion de publicaciones desde el frontend con acceso desde perfil y detalle usando `PUT /api/posts/:id`.
- Implementada eliminacion de publicaciones desde `Profile` en el frontend usando `DELETE /api/posts/:id` con confirmacion y mensajes visibles.
- Conectados favoritos en la interfaz para marcar y desmarcar publicaciones desde el listado y el detalle.
- Habilitada la edicion de `status`, `category` y `location` desde la interfaz de crear y editar publicaciones con opciones cerradas.

## Prioridad media

## Prioridad baja
- Agregar pruebas para los flujos pendientes del frontend.

## Observaciones tecnicas
- `GET /api/posts/:id` ahora reconoce token opcional para devolver `isFavorite` correctamente en el detalle.
- El carrito requiere que la base de datos tenga aplicado el cambio de esquema para `stock` en `posts` y la tabla `cart_items`.
